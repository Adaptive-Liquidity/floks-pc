interface:
  display_name: "Floks Pc"
  short_description: "Repo-specific patterns and workflows for floks-pc"
  default_prompt: "Use the floks-pc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true